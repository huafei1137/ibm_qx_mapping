// file: qelib1.inc







cx a,b;
cx a,b;
}
{
  h c;
  cx a,b;
}
{
  u1(lambda/2) b;
  cx a,b;
  u1(-lambda/2) b;
  cx a,b;
}
{
  u1(lambda/2) a;
  cx a,b;
  u1(-lambda/2) b;
  cx a,b;
  u1(lambda/2) b;
}
{
}
